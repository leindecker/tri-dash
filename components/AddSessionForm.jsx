// AddSessionForm removed: manual activity creation feature has been disabled.
// The component is kept as a no-op to avoid import errors if referenced elsewhere.

'use client';

export default function AddSessionForm() {
  return null;
}
